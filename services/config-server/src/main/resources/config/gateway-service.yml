server:
  port: 8090

spring:
  application:
    name: gateway-service

  profiles:
    active: docker

eureka:
  instance:
    preferIpAddress: false
    hostname: gateway-service
    nonSecurePort: 8090

  client:
    registerWithEureka: true
    fetchRegistry: true
